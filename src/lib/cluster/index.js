/**
 * @author Vinicius Figueiredo <vinifig@hotmail.com>
 * @module vf-clusterizer
 */

import Cluster from './cluster';
import ClusterManager from './cluster-manager';

export default {
  /**
   * @property Cluster {Cluster} The {@link Cluster} class
   */
  Cluster,
  /**
   * @property ClusterManager {ClusterManager} The {@link ClusterManager} class
   */
  ClusterManager
};
